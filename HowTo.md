[ウェブアプリケーションの起動]
1. 仮想環境をアクティベートします。
   ```bash
   source .venv/bin/activate
   ```
2. ウェブアプリケーションを起動します。
   ```bash
   python app.py
   ```
3. ブラウザで `http://localhost:5000` にアクセスして、ウェブアプリケーションが正常に動作していることを確認します。
4. アプリケーションを停止するには、ターミナルで `Ctrl + C` を押します。
5. 仮想環境を終了するには、以下のコマンドを実行します。
   ```bash
   deactivate
   ``` 
